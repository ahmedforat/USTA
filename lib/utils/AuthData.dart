class AuthData{
  String init; // hold the init page just in case the user did not complete the signUp process;
  String token; // hold the token for Authentication
  String test;
AuthData({this.init,this.token,this.test});
}